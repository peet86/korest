services:
  korest:
    build: .
    image: korest:latest
    container_name: korest
    ports:
      - "4242:4242"
    volumes:
      - korest-data:/data
    restart: unless-stopped

volumes:
  korest-data:
