{
  "name": "korest",
  "version": "2.0.0",
  "private": true,
  "description": "KoRest — self-hosted KOReader sync for Readest (kosync-compatible API, SQLite)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "fastify": "^5.2.1"
  }
}
